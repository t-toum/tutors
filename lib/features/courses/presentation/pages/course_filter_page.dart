import 'package:flutter/material.dart';

class CourseFilterPage extends StatelessWidget {
  const CourseFilterPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}